# 用户对话完整流程架构图

下面这张图描述 `DeepTutorSerevr` 中“用户发起一次对话”从入口、编排、执行、流式回传、持久化到恢复/取消的完整路径。

## 1. 完整架构图

```mermaid
flowchart TB
  U[用户 / Web UI / CLI] --> A[API / WebSocket 入口]
  A --> AU[认证与用户上下文]
  AU --> R[TurnRuntimeManager.start_turn]
  R --> S[Session Store<br/>ensure_session / create_turn / update_preferences]
  R --> C[UnifiedContext 构建]
  C --> O[ChatOrchestrator.handle]
  O --> CR[Capability Registry]
  CR --> CAP[具体 Capability<br/>chat / deep_solve / mastery / subagent ...]
  O --> BUS[StreamBus]
  CAP --> BUS

  BUS -->|stream| E1[StreamEvent<br/>thinking / content / tool_call / result]
  BUS -->|pause| E2[WAIT_FOR_INPUT / ask_user]
  BUS -->|error| E3[ERROR]
  BUS -->|done| E4[DONE]

  E1 --> WS[WebSocket / SSE / CLI 订阅端]
  E2 --> WS
  E3 --> WS
  E4 --> WS

  CAP --> T[Tool Registry]
  CAP --> L[LLM Service / Provider]
  CAP --> KB[Knowledge Base / RAG]
  CAP --> M[Memory / Notebook / Book / Partners / Skills]

  T --> L
  T --> KB
  T --> M

  BUS --> P[Turn Runtime 持久化事件]
  P --> S
  S --> H[Session History / Message Store]
  H --> U

  WS -->|cancel_turn| X[TurnRuntimeManager.cancel_turn]
  WS -->|submit_user_reply| Y[TurnRuntimeManager.submit_user_reply]
  WS -->|regenerate| Z[TurnRuntimeManager.regenerate_last_turn]
  WS -->|resume_from| Q[Turn replay / catch-up]
```

## 2. 一次对话的主流程

```mermaid
sequenceDiagram
  participant User as 用户
  participant WS as WebSocket API
  participant TR as TurnRuntimeManager
  participant SS as Session Store
  participant OR as ChatOrchestrator
  participant CAP as Capability
  participant BUS as StreamBus
  participant UI as 前端订阅端

  User->>WS: 发送 message / start_turn
  WS->>TR: start_turn(payload)
  TR->>SS: ensure_session / update_preferences / create_turn
  TR->>OR: 组装 UnifiedContext 后进入 handle()
  OR->>CAP: capability.run(context, bus)
  CAP->>BUS: emit thinking / tool_call / content / result
  BUS->>UI: WebSocket 推送流式事件
  CAP->>BUS: emit DONE 或 ERROR
  BUS->>UI: 收到终态事件
  TR->>SS: 落盘 turn / events / session 状态
  UI->>WS: 如需继续，发送 cancel / reply / resume / regenerate
```

## 3. 流程拆解

### 3.1 入口层

- `deeptutor/api/routers/unified_ws.py` 提供统一 WebSocket 入口。
- `deeptutor/api/routers/chat.py` 提供旧版 chat WebSocket 入口。
- CLI / SDK / 其他适配层最终都会落到 `deeptutor/app/facade.py` 或 `TurnRuntimeManager`。

### 3.2 编排层

- `deeptutor/services/session/turn_runtime.py` 负责 session、turn、偏好设置、恢复、取消、重放。
- `deeptutor/core/context.py` 负责统一上下文对象 `UnifiedContext`。
- `deeptutor/runtime/orchestrator.py` 负责把上下文路由到具体 capability。

### 3.3 执行层

- Capability 通过 `StreamBus` 持续输出事件。
- Capability 内部可以调用 `ToolRegistry`、LLM、RAG、记忆、Notebook、Book、伙伴、技能与子代理。
- `StreamEventType` 统一定义了 `thinking`、`content`、`tool_call`、`tool_result`、`error`、`done`、`wait_for_input` 等事件。

### 3.4 回传与恢复

- `StreamBus` 将事件实时广播给订阅者。
- WebSocket 端支持：
  - `subscribe_turn`
  - `subscribe_session`
  - `resume_from`
  - `cancel_turn`
  - `submit_user_reply`
  - `regenerate`
- `TurnRuntimeManager` 会在断线或重启后回放已持久化事件，并对孤儿 running turn 做恢复或终止处理。

## 4. 关键代码位置

- 入口与 facade: `deeptutor/app/facade.py`
- 统一编排: `deeptutor/runtime/orchestrator.py`
- 对话上下文: `deeptutor/core/context.py`
- 流式事件协议: `deeptutor/core/stream.py`
- 流式总线: `deeptutor/core/stream_bus.py`
- Turn 运行时: `deeptutor/services/session/turn_runtime.py`
- 会话管理: `deeptutor/services/session/unified_session_manager.py`
- 统一 WebSocket: `deeptutor/api/routers/unified_ws.py`
- 旧版 Chat WebSocket: `deeptutor/api/routers/chat.py`

## 5. 这条链路里的几个关键点

- 用户请求不是直接进 capability，而是先经过 `TurnRuntimeManager` 做会话与 turn 编排。
- 流式输出不是一次性返回，而是通过 `StreamBus` 逐段广播。
- `DONE` 和 `ERROR` 都是终态事件，前端需要依赖它们收敛 loading 状态。
- `wait_for_input` 说明这条链路支持中途暂停，等待用户继续回答。
- `resume_from` 和 `subscribe_turn` 说明这套对话是可恢复、可回放的，不是纯粹的一次性流式接口。

